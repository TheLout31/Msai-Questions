
function extractAndReverse(arr) {

   const sliced = arr.slice(3,5)
    const reverseit = sliced.reverse()
    return reverseit
}

const arr = [15, 30, 45, 60, 75, 90]
const functionList = extractAndReverse(arr);

